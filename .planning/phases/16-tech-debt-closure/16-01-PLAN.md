---
phase: 16-tech-debt-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tier3.rs
  - src/grpc.rs
  - src/mcp.rs
autonomous: true
requirements: [DEBT-01, DEBT-02, DEBT-03, DEBT-04, DEBT-05, DEBT-06, DEBT-07]

must_haves:
  truths:
    - "AnthropicClient implements LlmClient trait and is constructible"
    - "AnthropicClient is exported from krabnet::AnthropicClient public API"
    - "krabnet-server detects ANTHROPIC_API_KEY env var at startup"
    - "gRPC GetStats includes compaction metrics when compaction worker is configured"
    - "MCP krabnet_stats includes compaction metrics when compaction worker is configured"
    - "MCP server with WAL persists ingest events to disk"
    - "MCP WAL file can be replayed to recover ingested events"
  artifacts:
    - path: "src/tier3.rs"
      provides: "AnthropicClient struct implementing LlmClient trait + verification test"
      contains: "impl LlmClient for AnthropicClient"
    - path: "src/lib.rs"
      provides: "Public re-export of AnthropicClient"
      contains: "pub use tier3::{AnthropicClient"
    - path: "src/bin/krabnet-server.rs"
      provides: "ANTHROPIC_API_KEY env var detection and AnthropicClient construction"
      contains: "std::env::var(\"ANTHROPIC_API_KEY\")"
    - path: "src/grpc.rs"
      provides: "CompactionStats in GetStats response + verification test"
      contains: "compactions_completed"
    - path: "src/mcp.rs"
      provides: "CompactionStats in krabnet_stats + WAL persistence + verification tests"
      contains: "McpServer::with_wal"
    - path: "proto/krabnet.proto"
      provides: "4 optional compaction fields in GetStatsResponse"
      contains: "optional uint64 compactions_completed"
  key_links:
    - from: "src/tier3.rs"
      to: "src/lib.rs"
      via: "pub use re-export"
      pattern: "pub use tier3::\\{AnthropicClient"
    - from: "src/bin/krabnet-server.rs"
      to: "src/tier3.rs"
      via: "AnthropicClient construction from env var"
      pattern: "AnthropicClient::new\\(api_key"
    - from: "src/grpc.rs"
      to: "src/engine.rs"
      via: "engine.compaction_stats() -> GetStatsResponse fields"
      pattern: "engine\\.compaction_stats\\(\\)"
    - from: "src/mcp.rs"
      to: "src/wal.rs"
      via: "WalWriter for event persistence and WalReader for replay"
      pattern: "wal_writer.*append|WalReader::replay"
---

<objective>
Write targeted verification tests for all 7 DEBT requirements (DEBT-01 through DEBT-07), proving the already-built tech debt closure code works correctly.

Purpose: All tech debt code (677 lines across 10 files) is already in the working tree and compiles. This plan adds test coverage that exercises each DEBT requirement, then commits everything.

Output: 4-5 new test functions in tier3.rs, grpc.rs, and mcp.rs that verify AnthropicClient trait impl, lib.rs export, CompactionStats in gRPC/MCP, and MCP WAL persistence with replay.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-tech-debt-closure/16-RESEARCH.md

@src/tier3.rs
@src/lib.rs
@src/grpc.rs
@src/mcp.rs
@src/bin/krabnet-server.rs
@src/bin/krabnet-mcp.rs
@proto/krabnet.proto
@src/engine.rs
@src/wal.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verification tests for AnthropicClient and gRPC CompactionStats</name>
  <files>src/tier3.rs, src/grpc.rs</files>
  <action>
Add verification tests to existing `#[cfg(test)]` modules. IMPORTANT: cargo is located at `C:/Users/farha/.cargo/bin/cargo.exe` -- use full path or set PATH accordingly for all cargo commands.

**In src/tier3.rs** -- add to the existing `mod tests` block (after the `test_serialize_prompt_format` test):

1. `test_anthropic_client_implements_llm_client` (DEBT-01 + DEBT-07): Create an `AnthropicClient::new("test-key".to_string(), "test-model".to_string(), 100)`. Verify it satisfies the `LlmClient` trait bound by assigning to `let _: Box<dyn LlmClient> = Box::new(client);`. This is a compile-time proof that the trait is implemented. Also verify the struct fields are stored correctly by checking that construction does not panic. Add a second assertion that `krabnet::AnthropicClient` is accessible (use `use crate::AnthropicClient;` which goes through the lib.rs re-export path -- this verifies DEBT-07).

2. `test_anthropic_client_env_var_pattern` (DEBT-02): This is a code-inspection verification. Add a test that documents DEBT-02 is verified by the krabnet-server binary's use of `std::env::var("ANTHROPIC_API_KEY")`. The test itself should verify that `AnthropicClient::new` accepts the same parameter types that `std::env::var` returns (String). Create a client with a dummy key string and verify it is `Send + Sync` (required for the binary's `Box<dyn LlmClient>` usage) by asserting: `fn assert_send_sync<T: Send + Sync>() {} assert_send_sync::<AnthropicClient>();`

**In src/grpc.rs** -- add to the existing `mod tests` block (after the `test_ingest_broadcasts_and_tier3` test):

3. `test_grpc_stats_include_compaction_fields` (DEBT-03): Create an engine with compaction enabled using `Engine::with_config(64, Some(10_000), None, None)`. Wrap in `Arc<RwLock<_>>`. Create a `KrabnetServer`. Start the gRPC server on `[::1]:0`, connect a client, call `get_stats(GetStatsRequest{})`, and assert that the response has `compactions_completed == Some(0)`, `compaction_tuples_before == Some(0)`, `compaction_tuples_after == Some(0)`, and `total_compaction_time_us == Some(0)` (initial stats are zero but present). This proves the proto fields exist and are populated from `engine.compaction_stats()`. Mark this `#[tokio::test]`. Follow the exact same test setup pattern as `test_grpc_ingest_and_query` (bind, spawn, connect, call, abort).
  </action>
  <verify>
    <automated>C:/Users/farha/.cargo/bin/cargo.exe test --lib -- tier3::tests::test_anthropic_client_implements_llm_client tier3::tests::test_anthropic_client_env_var_pattern grpc::tests::test_grpc_stats_include_compaction_fields --exact 2>&amp;1</automated>
    <manual>All 3 new tests pass, proving DEBT-01, DEBT-02, DEBT-03, DEBT-07</manual>
  </verify>
  <done>
    - test_anthropic_client_implements_llm_client passes: AnthropicClient constructs and satisfies LlmClient trait bound (DEBT-01), accessible via crate re-export (DEBT-07)
    - test_anthropic_client_env_var_pattern passes: AnthropicClient is Send+Sync, accepts String params matching env var output (DEBT-02 structural verification)
    - test_grpc_stats_include_compaction_fields passes: GetStats response includes all 4 compaction fields when compaction worker is configured (DEBT-03)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verification tests for MCP CompactionStats and WAL persistence</name>
  <files>src/mcp.rs</files>
  <action>
Add verification tests to the existing `#[cfg(test)] mod tests` block in src/mcp.rs (after the `test_mcp_unknown_tool` test).

1. `test_mcp_stats_include_compaction_fields` (DEBT-04): Create an engine with compaction enabled using `Engine::with_config(64, Some(10_000), None, None)`. Create an `McpServer::new(engine)`. Send a `tools/call` request for `krabnet_stats` (follow the exact pattern from `test_mcp_ingest_and_query`). Parse the response content text as JSON. Assert that the response object contains `compactions_completed`, `compaction_tuples_before`, `compaction_tuples_after`, and `total_compaction_time_us` fields (values should be 0 for a fresh engine with no compaction triggered yet, but the keys must be present).

2. `test_mcp_wal_persistence_and_replay` (DEBT-05, DEBT-06): This is the integration test for WAL in MCP.
   - Create a temp directory using `std::env::temp_dir().join(format!("krabnet_mcp_wal_test_{}", std::process::id()))`.
   - Create a WAL path inside it: `temp_dir.join("test.wal")`.
   - Create an engine with `Engine::new(64)`.
   - Create a `WalWriter::new(&wal_path, 100).unwrap()`.
   - Create an `McpServer::with_wal(engine, wal_writer)`.
   - Ingest 2 NodeAdded events and 1 EdgeAdded event via `handle_request()` (same JSON-RPC pattern as other MCP tests).
   - Drop the server (this should flush the WAL -- the `run()` method flushes on exit, but since we're calling `handle_request()` directly, we need to ensure the WAL is flushed. After all ingests, access the server's internal state is not possible directly, so instead: after the 3 ingests, call `drop(server)` and then verify the WAL file exists and has content.
   - Actually, since `McpServer` doesn't expose `flush()` publicly, and `run()` flushes on exit, but we're not calling `run()`, we should verify WAL writes happen on each ingest (since `tool_ingest` calls `wal.append()` for each event). The WAL writer buffers internally and flushes every N events (100 in our case). With only 3 events, the buffer may not flush to disk. To handle this: after creating the server and ingesting 3 events, drop the McpServer. The WalWriter's Drop impl should flush. If it doesn't have a Drop impl, we need a different approach.
   - Safer approach: Instead of going through McpServer, verify WAL integration more directly. Create the WalWriter, create McpServer::with_wal(), ingest 3 events via handle_request(), then verify by reading the WAL file. Since WalWriter flushes every `sync_interval` events (100), and we only write 3, we need the Drop to flush OR we need to write 100+ events. Write 3 events, drop the server, then use `WalReader::replay(&wal_path)` to verify recovery. If the WalWriter's sync_interval means data isn't flushed with only 3 events, set sync_interval to 1: `WalWriter::new(&wal_path, 1)` so every event triggers a sync.
   - Use `WalWriter::new(&wal_path, 1)` (sync every 1 event) to ensure events are flushed to disk immediately.
   - After ingesting 3 events, drop the server.
   - Call `WalReader::replay(&wal_path).unwrap()` and assert it returns 3 entries.
   - Verify the replayed events match what was ingested (check event types/node IDs).
   - Clean up: `std::fs::remove_dir_all(&temp_dir).ok();`

   Note: Import `crate::wal::{WalWriter, WalReader}` at the top of the test module.
  </action>
  <verify>
    <automated>C:/Users/farha/.cargo/bin/cargo.exe test --lib -- mcp::tests::test_mcp_stats_include_compaction_fields mcp::tests::test_mcp_wal_persistence_and_replay --exact 2>&amp;1</automated>
    <manual>Both new MCP tests pass, proving DEBT-04, DEBT-05, DEBT-06</manual>
  </verify>
  <done>
    - test_mcp_stats_include_compaction_fields passes: krabnet_stats response includes all 4 compaction fields when compaction worker is configured (DEBT-04)
    - test_mcp_wal_persistence_and_replay passes: McpServer::with_wal() persists ingest events to WAL file (DEBT-06), and WalReader::replay() recovers them (DEBT-05)
  </done>
</task>

<task type="auto">
  <name>Task 3: Full test suite verification and commit</name>
  <files>src/tier3.rs, src/grpc.rs, src/mcp.rs, src/lib.rs, src/bin/krabnet-server.rs, src/bin/krabnet-mcp.rs, proto/krabnet.proto, Cargo.toml, Cargo.lock</files>
  <action>
Run the full test suite to verify zero regressions, then commit all changes.

1. Run `C:/Users/farha/.cargo/bin/cargo.exe test --lib` and verify all tests pass (should be 182+ now with the 2-5 new tests added in Tasks 1-2, plus existing 180).

2. Run `C:/Users/farha/.cargo/bin/cargo.exe test --doc` and verify all doc-tests pass (should be 54+, the new `AnthropicClient` doc example is `no_run` so it compiles but does not execute).

3. Run `C:/Users/farha/.cargo/bin/cargo.exe clippy -- -D warnings` and verify zero warnings.

4. If all pass, commit all changes in two logical commits:

   **Commit 1** -- LLM integration + exports (DEBT-01, DEBT-02, DEBT-07):
   Stage: `src/tier3.rs`, `src/lib.rs`, `src/bin/krabnet-server.rs`, `Cargo.toml`, `Cargo.lock`
   Message: `feat(16): add AnthropicClient LLM integration with env-var detection [DEBT-01, DEBT-02, DEBT-07]`

   **Commit 2** -- CompactionStats + WAL (DEBT-03, DEBT-04, DEBT-05, DEBT-06):
   Stage: `proto/krabnet.proto`, `src/grpc.rs`, `src/mcp.rs`, `src/bin/krabnet-mcp.rs`
   Message: `feat(16): add CompactionStats to gRPC/MCP and WAL persistence to MCP binary [DEBT-03, DEBT-04, DEBT-05, DEBT-06]`

   Note: Do NOT stage `.planning/config.json` in these commits -- it is a planning artifact, not source code.
  </action>
  <verify>
    <automated>C:/Users/farha/.cargo/bin/cargo.exe test --lib 2>&amp;1 &amp;&amp; C:/Users/farha/.cargo/bin/cargo.exe test --doc 2>&amp;1</automated>
    <manual>All lib tests (182+) and doc-tests (54+) pass. Zero clippy warnings. Two clean commits with DEBT-XX traceability.</manual>
  </verify>
  <done>
    - All lib tests pass (182+ including new verification tests)
    - All doc-tests pass (54+)
    - Zero clippy warnings
    - Two commits created with DEBT-01 through DEBT-07 traceability
    - No regressions in existing test suite
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `C:/Users/farha/.cargo/bin/cargo.exe test --lib` -- all 182+ tests pass
2. `C:/Users/farha/.cargo/bin/cargo.exe test --doc` -- all 54+ doc-tests pass
3. `C:/Users/farha/.cargo/bin/cargo.exe clippy -- -D warnings` -- zero warnings
4. `git log --oneline -3` -- shows two feat(16) commits with DEBT-XX references
5. `git diff --stat` -- only `.planning/config.json` remains as unstaged (planning artifact)
</verification>

<success_criteria>
- AnthropicClient constructs, satisfies LlmClient trait, is Send+Sync (DEBT-01 verified by test)
- krabnet-server binary compiles with ANTHROPIC_API_KEY detection (DEBT-02 verified by build + structural test)
- gRPC GetStats response includes 4 compaction fields when compaction worker configured (DEBT-03 verified by test)
- MCP krabnet_stats response includes 4 compaction fields when compaction worker configured (DEBT-04 verified by test)
- MCP WAL replay recovers ingested events after server restart (DEBT-05 verified by test)
- MCP WAL persists events during live ingest via McpServer::with_wal() (DEBT-06 verified by test)
- AnthropicClient exported from krabnet::AnthropicClient (DEBT-07 verified by test using crate re-export)
- Full test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/16-tech-debt-closure/16-01-SUMMARY.md`
</output>
