---
phase: 15-harden-mcp-binary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bin/krabnet-mcp.rs
  - src/mcp.rs
  - benches/krabnet_bench.rs
autonomous: true
requirements:
  - COMPACT-01
  - COMPACT-03
  - COALESCE-01
  - FANOUT-01

must_haves:
  truths:
    - "krabnet-mcp binary creates engine with compaction, coalescing, and fanout protection"
    - "Enterprise benchmarks exercise the full hardened engine stack at scale"
    - "MCP binary compiles and runs correctly with hardened config"
  artifacts:
    - path: "src/bin/krabnet-mcp.rs"
      provides: "MCP binary entry point with Engine::with_config()"
      contains: "Engine::with_config"
    - path: "benches/krabnet_bench.rs"
      provides: "Enterprise-scale benchmarks with hardened engine"
      contains: "Engine::with_config"
  key_links:
    - from: "src/bin/krabnet-mcp.rs"
      to: "src/engine.rs"
      via: "Engine::with_config() constructor"
      pattern: "Engine::with_config"
    - from: "benches/krabnet_bench.rs"
      to: "src/engine.rs"
      via: "Engine::with_config() in setup_scale_engine()"
      pattern: "Engine::with_config"
---

<objective>
Apply Phase 11 hardening features (background compaction, mutation coalescing, fan-out limits) to the MCP binary and update enterprise benchmarks to use realistic Engine::with_config() configuration.

Purpose: Close the final v2.0 milestone gap -- the MCP binary currently uses Engine::new() which skips all hardening, and enterprise benchmarks (BENCH-04, BENCH-05) test against an unhardened engine, making their results unrealistic.
Output: Hardened krabnet-mcp binary, updated enterprise benchmarks, all tests passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v2.0-MILESTONE-AUDIT.md
@src/bin/krabnet-mcp.rs
@src/bin/krabnet-server.rs
@src/mcp.rs
@benches/krabnet_bench.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden krabnet-mcp binary with Engine::with_config()</name>
  <files>src/bin/krabnet-mcp.rs, src/mcp.rs</files>
  <action>
In src/bin/krabnet-mcp.rs, replace:
```rust
let engine = Engine::new(1024);
```
with:
```rust
let engine = Engine::with_config(
    1024,         // ring buffer capacity
    Some(10_000), // compaction threshold (COMPACT-01, COMPACT-03)
    Some(16),     // coalescing window (COALESCE-01)
    Some(1000),   // max fanout (FANOUT-01)
);
```
Update the comment to explain: "Hardened engine with background compaction, mutation coalescing, and fan-out limits. Matches krabnet-server configuration for consistent behavior."

In src/mcp.rs, update the doc example (lines 26-31) to use Engine::with_config() instead of Engine::new(1024) so documentation reflects the correct usage pattern:
```rust
/// let engine = Engine::with_config(1024, Some(10_000), Some(16), Some(1000));
/// let mut server = McpServer::new(engine);
/// server.run().unwrap();
```

In src/mcp.rs tests, the existing tests use Engine::new(64) which is fine for unit tests -- they test MCP JSON-RPC protocol correctness, not engine hardening. Leave them as-is. The binary entry point is where hardening matters.

Do NOT change McpServer::new() signature or add any new fields to McpServer. The hardening is entirely in how Engine is constructed, not in MCP protocol logic.
  </action>
  <verify>
    <automated>cd C:/Users/farha/krabnet && PATH="$HOME/.cargo/bin:$PATH" cargo build --bin krabnet-mcp 2>&1 | tail -5 && PATH="$HOME/.cargo/bin:$PATH" cargo test --lib mcp 2>&1 | tail -10</automated>
    <manual>Verify krabnet-mcp binary compiles and all MCP tests pass</manual>
  </verify>
  <done>krabnet-mcp binary uses Engine::with_config() with compaction (10K threshold), coalescing (16 epoch window), and fanout (1000 max). Binary compiles. All MCP tests pass. Doc example updated.</done>
</task>

<task type="auto">
  <name>Task 2: Update enterprise benchmarks to use Engine::with_config()</name>
  <files>benches/krabnet_bench.rs</files>
  <action>
In benches/krabnet_bench.rs, update the setup_scale_engine() function (line 143) to replace:
```rust
let mut eng = Engine::new(2048);
```
with:
```rust
let mut eng = Engine::with_config(
    2048,         // ring buffer capacity (larger for scale)
    Some(10_000), // compaction threshold
    Some(16),     // coalescing window
    Some(1000),   // max fanout
);
```
Update the comment above the line to explain: "Hardened engine matching production config for realistic benchmarks."

This ensures BENCH-04 (bench_scale_ingest) and BENCH-05 (bench_scale_frame_query) exercise the full hardened stack including compaction, coalescing, and fanout at enterprise scale.

Do NOT change any other benchmark functions. Only setup_scale_engine() uses Engine::new() -- the smaller setup_engine() helper is used for basic benchmarks and is fine with Engine::new().

After the change, run `cargo bench --list` to verify all 13 benchmarks still compile and are discoverable.
  </action>
  <verify>
    <automated>cd C:/Users/farha/krabnet && PATH="$HOME/.cargo/bin:$PATH" cargo bench --list 2>&1 | tail -20 && PATH="$HOME/.cargo/bin:$PATH" cargo test --lib 2>&1 | tail -5</automated>
    <manual>Verify all 13 benchmarks appear in list and all tests still pass</manual>
  </verify>
  <done>setup_scale_engine() uses Engine::with_config() with compaction, coalescing, and fanout. All 13 benchmarks compile and list. All lib tests pass with zero regressions.</done>
</task>

</tasks>

<verification>
Run full quality gate:
1. `cargo build --bin krabnet-mcp` -- binary compiles
2. `cargo test --lib` -- all tests pass
3. `cargo test --doc` -- all doc-tests pass
4. `cargo clippy -- -D warnings` -- zero warnings
5. `cargo bench --list` -- all 13 benchmarks listed
6. Verify Engine::with_config appears in: krabnet-mcp.rs, krabnet_bench.rs (setup_scale_engine)
7. Verify Engine::new does NOT appear in: krabnet-mcp.rs
</verification>

<success_criteria>
- krabnet-mcp binary uses Engine::with_config(1024, Some(10_000), Some(16), Some(1000))
- setup_scale_engine() uses Engine::with_config(2048, Some(10_000), Some(16), Some(1000))
- All tests pass (lib + doc), zero clippy warnings
- All 13 benchmarks compile and list
- No regressions in any existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/15-harden-mcp-binary/15-01-SUMMARY.md`
</output>
