---
phase: 13-scale-and-optimize
plan: 02
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - src/trunk.rs
  - src/buffer_pool.rs
  - src/engine.rs
  - src/lib.rs
autonomous: true
requirements:
  - TRUNK-01
  - TRUNK-02
  - BUFPOOL-01
  - BUFPOOL-02
  - CMS-02
  - TEST-28
  - TEST-29
  - TEST-30

must_haves:
  truths:
    - "Trunk detection identifies sub-paths shared across multiple frames"
    - "Trunk frames are pinned to Hot tier and cannot be evicted"
    - "Buffer pool allocates and frees fixed-size pages from a contiguous buffer"
    - "Buffer pool evicts pages in Cold-first, then Warm, never Hot order"
    - "Engine uses FrameActivityTracker (CMS-backed) for all priority_score calls instead of per-frame counters"
    - "Engine calls detect_trunks post-registration and pins trunk frames to Hot"
    - "Engine instantiates BufferPool and uses evict_coldest for memory pressure relief"
  artifacts:
    - path: "src/trunk.rs"
      provides: "Trunk/leaf detection with Hot pinning"
      contains: "pub fn detect_trunks"
    - path: "src/buffer_pool.rs"
      provides: "Custom buffer pool with graph-aware eviction"
      contains: "pub struct BufferPool"
    - path: "src/engine.rs"
      provides: "Engine integration with CMS scoring, trunk pinning, and buffer pool eviction"
      contains: "FrameActivityTracker"
  key_links:
    - from: "src/trunk.rs"
      to: "src/engine.rs"
      via: "detect_trunks called on frame patterns, pinned_frame_ids blocks Hot demotion"
      pattern: "trunk::detect_trunks"
    - from: "src/buffer_pool.rs"
      to: "src/engine.rs"
      via: "BufferPool instantiated in engine, evict_coldest called for memory pressure"
      pattern: "buffer_pool::BufferPool"
    - from: "src/tiering.rs"
      to: "src/engine.rs"
      via: "FrameActivityTracker replaces per-frame counters in priority_score call sites"
      pattern: "FrameActivityTracker"
---

<objective>
Implement trunk/leaf detection for identifying structural spines shared across frames, and a custom buffer pool with graph-aware (tier-based) eviction ordering.

Purpose: Pin shared structural spines to Hot tier for cache efficiency, and manage memory with fine-grained page-level allocation that respects frame temperature for eviction.
Output: Two new modules (trunk.rs, buffer_pool.rs), engine.rs integration, comprehensive tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-scale-and-optimize/13-01-SUMMARY.md
@src/engine.rs
@src/frame.rs
@src/types.rs
@src/tiering.rs
@src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement trunk/leaf detection module</name>
  <files>src/trunk.rs, src/lib.rs</files>
  <action>
Create `src/trunk.rs` implementing trunk/leaf detection (TRUNK-01, TRUNK-02):

**Core concept:** A "trunk" is a sub-path (sequence of HopSpecs) that appears as a prefix or contiguous sub-sequence in multiple frame patterns. Frames sharing trunk paths benefit from Hot pinning since they're structural spines accessed by many queries.

**Data structures:**
```rust
/// Result of trunk detection: which sub-paths are trunks and which frames they span.
#[derive(Debug, Clone)]
pub struct TrunkInfo {
    /// Sub-path that qualifies as a trunk.
    pub path: Vec<HopSpec>,
    /// Frame IDs sharing this trunk.
    pub frame_ids: Vec<u64>,
}
```

**Public API (TRUNK-01):**
- `pub fn detect_trunks(frames: &[(u64, Vec<HopSpec>)], min_shared_count: usize) -> Vec<TrunkInfo>`
  - Takes (frame_id, pattern) pairs and a minimum sharing threshold.
  - For each frame pattern, generate all contiguous sub-paths of length >= 1 (use sliding window).
  - Count how many frames share each sub-path (use `HashMap<Vec<HopSpec>, Vec<u64>>`).
  - Return sub-paths with `frame_ids.len() >= min_shared_count` as TrunkInfo.
  - Sort results by frame count descending (most shared first).

- `pub fn classify_frames(frames: &[(u64, Vec<HopSpec>)], trunk_infos: &[TrunkInfo]) -> (HashSet<u64>, HashSet<u64>)`
  - Returns `(trunk_frame_ids, leaf_frame_ids)`.
  - A frame is a "trunk frame" if its pattern appears as a trunk sub-path (or contains one).
  - A frame is a "leaf frame" if it does NOT appear in any trunk.
  - All frames in any TrunkInfo.frame_ids are trunk frames; the rest are leaf frames.

**TRUNK-02 — Hot pinning:**
- `pub fn pinned_frame_ids(trunk_infos: &[TrunkInfo]) -> HashSet<u64>`
  - Returns the set of all frame IDs that participate in any trunk (these should be pinned to Hot).

**Implementation notes:**
- HopSpec must implement PartialEq + Eq + Hash for use as HashMap key. Check if it already does -- if not, derive these traits on HopSpec in types.rs (it currently derives Debug, Clone, Copy, PartialEq, Eq, Hash -- verify). Actually HopSpec has Option fields and Filter enum -- verify all are hashable. If Filter doesn't implement Hash, add it.
- Actually, HopSpec contains `filter: Filter` and Filter contains `PropertyValue` which has `f64` (Float variant) -- this means we CANNOT derive Hash on HopSpec directly. Instead, serialize the sub-path to a canonical string representation for HashMap keying:
  - `fn hop_key(hop: &HopSpec) -> String` — format as "{direction:?}|{edge_type:?}|{target_type:?}|{filter:?}"
  - `fn path_key(path: &[HopSpec]) -> String` — join hop_keys with "/"
  - Use `HashMap<String, Vec<u64>>` for counting.

**Tests (co-located in trunk.rs, TEST-28):**
- `test_detect_trunks_basic` — 5 frames, 3 share first 2 hops, detected as trunk
- `test_detect_trunks_50_frames` (TEST-28): 50 frames, 30 share first 2 hops, detected as trunk, pinned to Hot via `pinned_frame_ids`
- `test_classify_frames` — verify trunk vs leaf classification
- `test_no_trunks` — all unique patterns, no trunks detected
- `test_min_shared_count` — verify threshold filtering works

Update `src/lib.rs`:
- Add `pub mod trunk;`
- Add re-export: `pub use trunk::{TrunkInfo, detect_trunks};`
  </action>
  <verify>
    <automated>cargo test --lib -- trunk:: --nocapture 2>&amp;1 | tail -20</automated>
    <manual>All trunk detection tests pass including TEST-28 with 50 frames</manual>
  </verify>
  <done>detect_trunks correctly identifies sub-paths shared across >= min_shared_count frames. Trunk frames classified and pinned frame IDs computed. TEST-28 passes with 50 frames where 30 share first 2 hops.</done>
</task>

<task type="auto">
  <name>Task 2: Implement buffer pool with graph-aware eviction</name>
  <files>src/buffer_pool.rs, src/lib.rs</files>
  <action>
Create `src/buffer_pool.rs` implementing a custom buffer pool (BUFPOOL-01, BUFPOOL-02):

**BufferPool struct (BUFPOOL-01):**
```rust
pub struct BufferPool {
    /// Contiguous backing buffer.
    buffer: Vec<u8>,
    /// Size of each page in bytes.
    page_size: usize,
    /// Total number of pages.
    page_count: usize,
    /// Free list: indices of available pages.
    free_list: Vec<usize>,
    /// Allocated pages: maps page_index -> PageMeta.
    allocated: HashMap<usize, PageMeta>,
}

#[derive(Debug, Clone)]
pub struct PageMeta {
    /// Which frame owns this page (if any).
    pub frame_id: Option<u64>,
    /// Tier of the owning frame at allocation time.
    pub tier: FrameTier,
}

/// Handle returned by alloc, used for read/write/free.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub struct PageHandle(pub usize);
```

**Public API:**
- `pub fn new(total_bytes: usize, page_size: usize) -> Self`
  - Default: 256 MB total, 4 KB pages = 65,536 pages.
  - Allocate `vec![0u8; total_bytes]`, compute page_count, populate free_list with 0..page_count.
  - For testing, accept smaller sizes.

- `pub fn alloc(&mut self, meta: PageMeta) -> Option<PageHandle>`
  - Pop from free_list, store meta in allocated, return PageHandle.
  - Returns None if no free pages.

- `pub fn free(&mut self, handle: PageHandle)`
  - Remove from allocated, push index back to free_list.
  - Panics if handle not allocated (debug assert).

- `pub fn read(&self, handle: PageHandle, offset: usize, len: usize) -> &[u8]`
  - Returns slice from `buffer[page_start + offset .. page_start + offset + len]`.
  - Panics if offset+len > page_size or handle not allocated.

- `pub fn write(&mut self, handle: PageHandle, offset: usize, data: &[u8])`
  - Copies data into `buffer[page_start + offset ..]`.
  - Panics if offset+data.len() > page_size or handle not allocated.

- `pub fn free_page_count(&self) -> usize` — returns free_list.len()
- `pub fn allocated_page_count(&self) -> usize` — returns allocated.len()
- `pub fn total_page_count(&self) -> usize` — returns page_count

**Graph-aware eviction (BUFPOOL-02):**
- `pub fn evict_by_tier(&mut self, tier: FrameTier) -> Vec<PageHandle>`
  - Find all allocated pages with matching tier, free them, return their handles.
  - Used by engine to free Cold pages first, then Warm, never Hot.

- `pub fn evict_coldest(&mut self, count: usize) -> Vec<PageHandle>`
  - Evict up to `count` pages in priority order: Cold first, then Warm, never Hot.
  - Collect Cold pages, if not enough add Warm pages, never touch Hot.
  - Free each evicted page, return handles.

- `pub fn update_tier(&mut self, handle: PageHandle, new_tier: FrameTier)`
  - Update the tier in the page's metadata (e.g., after hysteresis tier change).

**Implementation notes:**
- Use `HashMap<usize, PageMeta>` for O(1) alloc/free by page index.
- Free list is a Vec used as a stack (push/pop from end) for O(1) alloc.
- Buffer is a single `Vec<u8>` -- page N starts at `N * page_size`.
- For testing, use small sizes (e.g., 4096 total bytes, 256 byte pages = 16 pages).

**Tests (co-located in buffer_pool.rs):**
- `test_alloc_free` (TEST-29): allocate all pages, free half, reallocate, verify no corruption by writing distinct patterns to each page and reading them back.
- `test_eviction_order` (TEST-30): allocate pages with Cold, Warm, and Hot tiers. Call `evict_coldest(N)`. Verify Cold evicted first, then Warm, Hot never evicted.
- `test_alloc_returns_none_when_full` — fill pool, verify alloc returns None.
- `test_read_write_roundtrip` — write bytes, read back, verify match.
- `test_update_tier` — allocate as Cold, update to Hot, verify evict_coldest skips it.

Update `src/lib.rs`:
- Add `pub mod buffer_pool;`
- Add re-export: `pub use buffer_pool::{BufferPool, PageHandle};`
  </action>
  <verify>
    <automated>cargo test --lib -- buffer_pool:: --nocapture 2>&amp;1 | tail -20</automated>
    <manual>All buffer pool tests pass including TEST-29 and TEST-30</manual>
  </verify>
  <done>BufferPool allocates/frees pages from pre-allocated contiguous buffer. Graph-aware eviction evicts Cold first, then Warm, never Hot. TEST-29 (alloc/free without corruption) and TEST-30 (eviction order) pass.</done>
</task>

<task type="auto">
  <name>Task 3: Wire CMS scoring, trunk pinning, and buffer pool into engine.rs</name>
  <files>src/engine.rs</files>
  <action>
**This task integrates the three new data structures into the live engine pipeline.** Requires 13-01 SUMMARY (FrameActivityTracker from tiering.rs) and Tasks 1-2 above (trunk.rs, buffer_pool.rs).

**1. Add FrameActivityTracker to Engine (CMS-02 — "instead of"):**

Add field to Engine struct:
```rust
activity_tracker: FrameActivityTracker,
```

Import `use crate::tiering::FrameActivityTracker;`

Initialize in `Engine::new()`, `Engine::with_compaction()`, and `Engine::with_config()`:
```rust
activity_tracker: FrameActivityTracker::new(),
```

**Replace per-frame counter usage in priority_score call sites:**

In `ingest()`, there are two calls to `crate::tiering::priority_score(frame.query_count(), frame.mutation_count(), ...)`. Replace BOTH with CMS-estimated counts:
- Before the fan-out scoring loop, call `self.activity_tracker.record_mutation(frame_id)` for each affected frame (the event triggers a mutation on these frames).
- Replace `frame.query_count()` with `self.activity_tracker.estimated_query_count(frame_id)` in both priority_score call sites.
- Replace `frame.mutation_count()` with `self.activity_tracker.estimated_mutation_count(frame_id)` in both priority_score call sites.

In `query_frame()`, call `self.activity_tracker.record_query(frame_id)` when a frame is queried.

This ensures the engine's tiering decisions are driven by CMS-estimated counts rather than per-frame counters, fulfilling CMS-02's "instead of" requirement.

**2. Add trunk pinning to Engine (TRUNK-02):**

Add fields to Engine struct:
```rust
/// Set of frame IDs pinned to Hot tier (trunk frames).
pinned_hot: HashSet<u64>,
```

Import `use crate::trunk::{detect_trunks, pinned_frame_ids};`

Initialize `pinned_hot: HashSet::new()` in all constructors.

**After frame registration in `register_frame()`:**
- Collect all registered frames' (frame_id, pattern) pairs from `self.frames`.
- Call `detect_trunks(&frame_patterns, 2)` (min_shared_count=2).
- Update `self.pinned_hot = pinned_frame_ids(&trunk_infos)`.

**In the hysteresis tier update section of `ingest()`:**
- After computing the new tier via hysteresis, check if the frame_id is in `self.pinned_hot`.
- If pinned, override the tier to `FrameTier::Hot` regardless of the hysteresis recommendation.
- This prevents trunk frames from being demoted below Hot.

**3. Add BufferPool to Engine (BUFPOOL-02):**

Add field to Engine struct:
```rust
/// Optional buffer pool for page-level memory management.
buffer_pool: Option<BufferPool>,
```

Import `use crate::buffer_pool::BufferPool;`

Initialize `buffer_pool: None` in `Engine::new()` and `Engine::with_compaction()`.

Add builder method:
```rust
pub fn with_buffer_pool(mut self, total_bytes: usize, page_size: usize) -> Self {
    self.buffer_pool = Some(BufferPool::new(total_bytes, page_size));
    self
}
```

**Memory pressure relief in `ingest()`:**
- After all frame evaluations complete, if `buffer_pool` is Some and `free_page_count() < total_page_count() / 10` (less than 10% free), call `evict_coldest(total_page_count() / 20)` to free 5% of pages.

Add public method for external memory pressure:
```rust
pub fn relieve_memory_pressure(&mut self, pages_to_free: usize) -> usize {
    if let Some(ref mut pool) = self.buffer_pool {
        pool.evict_coldest(pages_to_free).len()
    } else {
        0
    }
}
```

**Tests (co-located in engine.rs tests):**
- `test_engine_uses_cms_scoring` — Create engine, ingest events, verify that FrameActivityTracker has recorded mutations (check via a method or by verifying scoring behavior differs from zero-count baseline).
- `test_engine_trunk_pinning` — Register 5 frames where 3 share the same first 2 hops. Verify pinned_hot contains those 3 frame IDs. Ingest events that would normally demote a frame, verify trunk frames stay Hot.
- `test_engine_buffer_pool_eviction` — Create engine with small buffer pool (4096 bytes, 256-byte pages). Allocate pages, verify evict_coldest frees Cold pages first.

Ensure ALL existing engine tests still pass.
  </action>
  <verify>
    <automated>cargo test --lib -- engine:: trunk:: buffer_pool:: tiering:: --nocapture 2>&amp;1 | tail -30</automated>
    <manual>All existing engine tests pass. New integration tests verify CMS scoring, trunk pinning, and buffer pool eviction are wired into the engine pipeline.</manual>
  </verify>
  <done>Engine uses FrameActivityTracker for priority_score instead of per-frame counters (CMS-02). Trunk frames pinned to Hot and cannot be demoted (TRUNK-02). BufferPool instantiated in engine with evict_coldest for memory pressure (BUFPOOL-02). All existing + new tests pass.</done>
</task>

</tasks>

<verification>
1. `cargo test --lib -- trunk:: buffer_pool:: engine::` — all tests pass
2. `cargo clippy -- -D warnings` — zero warnings
3. TEST-28: 50 frames, 30 sharing first 2 hops → detected as trunk, pinned
4. TEST-29: allocate all, free half, reallocate, no data corruption
5. TEST-30: evict_coldest evicts Cold before Warm, never Hot
6. Engine priority_score calls use FrameActivityTracker CMS estimates, not per-frame counters
7. Trunk frames pinned to Hot in engine — cannot be demoted by hysteresis
8. Engine buffer pool evicts coldest pages under memory pressure
</verification>

<success_criteria>
- detect_trunks identifies shared sub-paths across frames with configurable min_shared_count threshold
- Trunk frames pinned to Hot tier via pinned_frame_ids
- BufferPool alloc/free/read/write works with contiguous pre-allocated buffer
- Eviction respects tier ordering: Cold first, Warm second, Hot never
- Engine uses FrameActivityTracker (CMS) for all priority_score calls (CMS-02 "instead of" fulfilled)
- Engine calls detect_trunks after register_frame and pins trunk frames to Hot (TRUNK-02 fulfilled)
- Engine instantiates BufferPool and calls evict_coldest for memory pressure (BUFPOOL-02 fulfilled)
- All 5+ tests in each module pass including TEST-28, TEST-29, TEST-30
- All existing engine tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/13-scale-and-optimize/13-02-SUMMARY.md`
</output>
