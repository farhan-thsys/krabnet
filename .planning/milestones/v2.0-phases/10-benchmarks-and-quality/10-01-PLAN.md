---
phase: 10-benchmarks-and-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - benches/krabnet_bench.rs
  - src/lib.rs
  - src/types.rs
  - src/interner.rs
  - src/sequencer.rs
  - src/ring_buffer.rs
  - src/graph.rs
  - src/diff.rs
  - src/frame.rs
  - src/routing.rs
  - src/interpret.rs
  - src/tiering.rs
  - src/embryonic.rs
  - src/engine.rs
autonomous: true
requirements:
  - BENCH-01
  - QUAL-01
  - QUAL-02
  - QUAL-03
  - QUAL-04
  - QUAL-05

must_haves:
  truths:
    - "Criterion benchmarks for ingest_event, frame_query, inverted_index_lookup, tier1_check, embryonic_observe, and compaction all execute and produce numbers"
    - "cargo test runs with zero failures across all modules"
    - "cargo clippy produces zero warnings"
    - "cargo doc --no-deps generates documentation without warnings"
    - "Every public function has a doc comment and every module has a module-level doc comment"
  artifacts:
    - path: "benches/krabnet_bench.rs"
      provides: "Criterion benchmarks for all critical operations"
      contains: "criterion_group"
      min_lines: 80
---

<objective>
Add Criterion benchmarks for all critical operations and ensure all quality gates pass.

Purpose: Phase 10 validates that the complete Krabnet crate meets production quality standards. Criterion benchmarks establish performance baselines for the hot path operations. Quality gates ensure clean compilation, test coverage, documentation, and lint compliance.

Output: `benches/krabnet_bench.rs` with 6 benchmarks, and all quality gates passing (tests, clippy, docs, doc coverage).
</objective>

<tasks>

<task id="1" name="Create Criterion benchmarks for critical operations" commit_prefix="feat(10-01)">
Replace the placeholder `benches/krabnet_bench.rs` with real benchmarks:

**Benchmark setup helper:** Create a pre-populated engine with ~100 nodes, ~200 edges, 5 registered frames, and 2 embryonic templates. This gives benchmarks realistic data to work with.

**Benchmarks:**
1. `bench_ingest_event` — benchmark `engine.ingest(Event::EdgeAdded { ... })` with a new edge between existing nodes
2. `bench_frame_query` — benchmark `engine.query_frame(frame_id)` on a materialized frame
3. `bench_inverted_index_lookup` — benchmark `index.affected_frames(&event)` on an index with multiple registered frames
4. `bench_tier1_check` — benchmark `interpret::tier1_check(prev, curr)` with varying deltas
5. `bench_embryonic_observe` — benchmark `embryonic.observe_edge(...)` with registered templates and existing candidates
6. `bench_compaction` — benchmark `engine.compact_all(frontier)` on frames with mixed assert/retract tuples

Use `criterion::black_box` to prevent dead code elimination.
Use `criterion_group!` and `criterion_main!` macros.

**Verification:** `cargo bench -- --test` (quick validation that benchmarks compile and run) — zero errors
</task>

<task id="2" name="Add missing doc comments and pass quality gates" commit_prefix="feat(10-01)">
Audit all source files for missing doc comments:

**For each source file (types.rs, interner.rs, sequencer.rs, ring_buffer.rs, graph.rs, diff.rs, frame.rs, routing.rs, interpret.rs, tiering.rs, embryonic.rs, engine.rs):**
- Ensure module-level `//!` doc comment exists
- Ensure every `pub fn`, `pub struct`, `pub enum`, `pub type` has `///` doc comment
- Add missing doc comments where needed (brief, one-line is fine for simple items)

**Quality gate checks:**
- `cargo test` — zero failures
- `cargo clippy` — zero warnings
- `cargo doc --no-deps` — zero warnings (use `2>&1 | grep -c warning` to verify)

**Fix any issues found during audit.**

**Verification:** All four quality gates pass
</task>

</tasks>
