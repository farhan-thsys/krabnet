---
phase: new-milestone-v3.0
task: 2
total_tasks: 11
status: in_progress
last_updated: 2026-02-26T13:35:34.689Z
---

<current_state>
Starting /gsd:new-milestone for v3.0. Completed Step 1 (Load Context) — read PROJECT.md, MILESTONES.md, STATE.md. No MILESTONE-CONTEXT.md exists. About to begin Step 2 (Gather Milestone Goals) but context window exhausted.
</current_state>

<completed_work>

- Step 1: Load Context - Done (PROJECT.md, MILESTONES.md, STATE.md all read)
- Spec analysis: FULL_PROJECT_SPEC.txt reviewed against codebase — comprehensive gap analysis complete
- v2.0 milestone: Fully completed, archived, tagged, pushed to github.com/farhan-thsys/krabnet
</completed_work>

<remaining_work>

- Step 2: Gather Milestone Goals (user already specified scope — see below)
- Step 3: Determine Milestone Version (v3.0 confirmed by user)
- Step 4: Update PROJECT.md with Current Milestone section
- Step 5: Update STATE.md for new milestone
- Step 6: Cleanup and Commit
- Step 7: Load Context and Resolve Models (init new-milestone)
- Step 8: Research Decision
- Step 9: Define Requirements (REQUIREMENTS.md with REQ-IDs)
- Step 10: Create Roadmap (spawn gsd-roadmapper, phases start at 16)
- Step 11: Done — present next steps
</remaining_work>

<decisions_made>

- Milestone version: v3.0 (user confirmed "start v3.0 with those items")
- Milestone scope derived from FULL_PROJECT_SPEC.txt gap analysis:
  - Wave 1 (quick wins): Fix 4 parameter mismatches to match spec, add ListFrames + EvictFrame MCP tools, extend stress test to 60s
  - Wave 2 (benchmark hardening): Run benchmarks against spec production targets with assertions, scale to 1M nodes / 10M edges, validate 100K+ events/sec
  - Wave 3 (optional): Multi-producer ring buffer with AtomicBool per slot
- Deferred (needs external resources): Distributed operation, real AnthropicClient (needs API key)
- Phase numbering continues from 16 (v2.0 ended at phase 15)
</decisions_made>

<blockers>
None — context window exhaustion only.
</blockers>

<context>
User added FULL_PROJECT_SPEC.txt to repo root — the original build specification. We compared it against the built codebase and identified:

**Parameter mismatches (trivial):**
- Ring buffer default: 1024 vs spec 8192
- Priority mutation_weight: 0.3 vs spec 0.35
- Priority recency_weight: 0.3 vs spec 0.25
- Max embryonic candidates/template: 100 vs spec 5000

**Missing MCP tools:** ListFrames, EvictFrame (gRPC already has both)

**Benchmark gaps:** Stress test is 500K events (~10s) not 60s sustained. No explicit assertions against spec p99 targets. Enterprise scale is 100K/1M not 1M/10M.

**Multi-producer ring buffer:** Current is single-writer (&mut self). Spec wants AtomicBool per slot for concurrent writers. Large effort, optional.

**Cannot build without resources:** Distributed operation (needs infra), AnthropicClient (needs API key)
</context>

<next_action>
Resume /gsd:new-milestone workflow at Step 2. User's scope is already known (3 waves from spec gap analysis). Skip questioning — go straight to confirming v3.0 version, updating PROJECT.md, then requirements + roadmap. Phase numbering starts at 16.
</next_action>
